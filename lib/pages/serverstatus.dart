import 'package:flutter/material.dart';

class PageServerStatus extends StatelessWidget {
  const PageServerStatus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('Server Status'),
        ),
        body: ListView(
          children: [],
        ));
  }
}
